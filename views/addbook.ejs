<% include layouts/main.ejs %>

<h1>Buch hinzufügen </h2>
<form method="POST" action="/books">
    <div class="form-group">
	<label>Buchtitel</label> <br>
	<input type="text" name="query"  placeholder="Buch eingeben">
    </div>

<label>Artikelbeschreibung hinzufügen</label> <br>
<textarea rows="4" cols="50" name="description" placehoder="Artikelbeschreibung"></textarea> <br>

<input type="submit" class="btn btn-primary" value="Buch hinzufügen">
</form>

</div>
<h2> Alle Bücher </h2>

<% book.forEach(function(book) { %>
<div class="container">
    <h2><a href="/books/<%= book.isbn %>"><%= book.title %></a></h2>
    <div class="row">
        <div class="col-md-12">
            <a href="/books/<%= book.isbn %>"><img style='float:left; margin-right:10px;' src="<%= book.thumbnail %>" /> </a>
            <p><%= book.name %></p>
	    <p><%= book.isbn %></p>
	 <div class="btn-group">
       	    <button type="button" class="btn btn-danger">Delete</button>
	    <button type="submit" value="recommend" class="btn btn-success">Recommend</button>
	 </div>
    </div>
<% }); %>